export enum InvoiceTypeEnums {
    A = 1,
    B = 2,
    C = 3
}


// export const ModelInvoiceTypeList: {
//     value: number;
//     label: string;
// }[]= Object.entries(InvoiceTypeEnums).map(([value, label])=>({ value, label}))